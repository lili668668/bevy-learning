[package]
name = "poker"
version = "0.1.0"
edition = "2024"

[workspace]
members = ["game_core"]

[dependencies]
bevy = "0.19.1"
bevy-inspector-egui = { version = "0.37.0", optional = true }
game_core = { path = "game_core" }

[features]
default = ["dev"]
dev = ["dep:bevy-inspector-egui"]
